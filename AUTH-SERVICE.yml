server:
  port: 8085

jwt:
  secret: secret

eureka:
  instance:
    pref-ip-address: true
  client:
    fetch-regitry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8880/eureka
